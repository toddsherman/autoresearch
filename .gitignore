# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
worktrees/
results/
queue/

# Agent prompt files (generated per-session by launchers)
CLAUDE.md
AGENTS.md

# Experimental code/artifacts
dev/

# Results file
results.tsv

# Telemetry (machine-generated per run, consumed by the playback site)
telemetry/
run.log
telemetry-night2/
probe_results.json

# Champion checkpoints backed up off RunPod before teardown (483MB of weights)
checkpoints/
